import palette from '../../palette';
import colors from '../../colors';

export default {
  root: {
    fontSize: 'inherit',
    color: 'inherit',
    '&:hover': {
      backgroundColor: 'inherit', // disables default hover animation
    },
  },
};
