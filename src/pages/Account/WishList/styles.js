export default function({ colors, values, breakpoints, palette }) {
  return {

    root: {
      display: 'flex',
      flexDirection: 'row',
    },

    wishContainer: {
      display: 'flex',
      flexGrow: 1,
    },
  }
}
