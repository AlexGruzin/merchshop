export default function({ colors, breakpoints, palette, values }) {
  return {

    root: {
      textDecoration: 'none',
      textDecorationColor: colors.BLACK,
      cursor: 'pointer',
    },
  }
}
