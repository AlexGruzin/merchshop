export default function({ colors }) {
  return {

    error: {
      color: colors.CORAL,
      textAlign: 'right',
      fontSize: '11px',
    },

    warning: {
      color: colors.ALERT,
    }
  }
}
