export default function({ colors, values, breakpoints, palette }) {
  return {
    select: {
      width: '100%',
      height: 'inherit',
    },

    root: {
      width: '100%',
    },

    formControl: {
    },

  }
}
