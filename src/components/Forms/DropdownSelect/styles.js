export default function({ colors, values, breakpoints, palette }) {
  return {
    select: {
      width: '100%',
      height: 'inherit',
      backgroundColor: colors.WHITE,
    },

    root: {
      width: '100%',
    },

    formControl: {
    },

  }
}
