export default ({ colors, values }) => ({
  root: {
    display: 'flex',
    alignSelf: 'stretch',
  },
  checkRoot: {
    padding: 0,
  },
  sizeIcon: {
    fontSize: values.GIANT_FONT_SIZE,
  },
});
